import 'package:flutter/material.dart';

class NatureList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('List Of Nature'),
    );
  }
}
